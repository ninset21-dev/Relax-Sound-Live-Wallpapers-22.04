// Kept for backward compatibility — engine is now defined inline inside
// LiveWallpaperService.kt (as an inner class, which is required because
// WallpaperService.Engine is a non-static inner class of WallpaperService).
exports.wallpaperEngineKt = (_pkg) => null;
